
include_tovsolver = include_directories('.')

headers_tovsolver = files('spherical_stars.h', 'star_sequence.h', 
                          'star_seq_file.h')

install_headers(headers_tovsolver, subdir : project_headers_dest)
